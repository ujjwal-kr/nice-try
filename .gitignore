# =========================================
# Git ignore for a Flask-based Python project
# Tailored to typical Flask workflows and the project's flake.nix (creates `env`)
# =========================================
/.direnv/
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
parts/
sdist/
var/
*.egg-info/
.installed.cfg

# Virtual environments and venvs
# Note: flake.nix's shellHook creates an `env` venv — ignore it.
env/
venv/
ENV/
.venv/
env.bak/
venv.bak/

# Python tooling caches
pip-wheel-metadata/
__pypackages__/
*.manifest
*.lock

# Flask-specific
instance/                 # Flask instance folder (contains secrets / config)
*.db                      # local SQLite DB files
*.sqlite3

# Environment files
.env
.env.*                    # .env.local, .env.production, etc.
.flaskenv
.flaskenv.*               # any flaskenv variants

# Test / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover
nosetests.xml
test-results/

# Type checkers / caches
.mypy_cache/
.pyre/
.pytype/

# Build / docs
_build/
docs/_build/

# Logs and runtime files
*.log
logs/
gunicorn-error.log
gunicorn-access.log

# Static asset caches
.webassets-cache/

# Editor / IDE files
.vscode/
.idea/
*.sublime-workspace
*.sublime-project
*.swp
*~
.project
.metadata
*.iml

# JetBrains
/.idea/
/*.iws
/*.ipr

# macOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Misc OS files
*.bak
*.old
*.orig

# Docker (ignore local docker env overrides)
docker-compose.override.yml
.env.docker
docker-compose.local.yml

# Node / front-end (if present)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
.pnpm-debug.log

# Coverage / test / other tool artifacts
.coverage.*
coverage/
.vtest/
htmlcov/

# Virtualenv created by pipenv/poetry
.pipfile
Pipfile.lock
poetry.lock

# Other caches
.cache/
.cache-pip/
.cache/
pip-log.txt

# Temporary files created by editors, OS, etc.
*.tmp
*.temp
*.cache

# End of file
.direnv

